import 'package:flutter/material.dart';

class AppColors {
  // Primary — deep ink blue (trust, depth, learning)
  static const primary = Color(0xFF1B2B4B);
  static const primaryLight = Color(0xFF2D4270);
  static const primarySurface = Color(0xFFEEF2FF);

  // Accent — warm amber (energy, highlight, reward)
  static const accent = Color(0xFFE8A020);
  static const accentLight = Color(0xFFFFF3D6);

  // Success — muted sage green
  static const success = Color(0xFF3D7A5E);
  static const successLight = Color(0xFFE6F4EE);

  // Error — warm red
  static const error = Color(0xFFD94F3D);
  static const errorLight = Color(0xFFFDECEA);

  // Neutrals — warm-tinted, not cold gray
  static const background = Color(0xFFF8F7F4);     // warm off-white
  static const surface = Color(0xFFFFFFFF);
  static const surfaceWarm = Color(0xFFF3F1EC);
  static const border = Color(0xFFE4E1D9);
  static const borderStrong = Color(0xFFCCC9BE);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6760);
  static const textTertiary = Color(0xFF9E9B95);
  static const textOnDark = Color(0xFFFFFFFF);

  // Skill colors (each skill has its own identity)
  static const reading = Color(0xFF3B5BA5);      // blue
  static const readingLight = Color(0xFFEEF2FF);
  static const writing = Color(0xFF7B4EA6);      // purple
  static const writingLight = Color(0xFFF4EEFF);
  static const listening = Color(0xFF2E8B72);    // teal
  static const listeningLight = Color(0xFFE6F7F3);
  static const speaking = Color(0xFFD4633A);     // terra cotta
  static const speakingLight = Color(0xFFFFF0EB);
}
